<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:wght@200;300;400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    />

    <style>
      .wrapper {
        background-color: #f1f1f1;
        padding: 1rem;
        border-radius: 1rem;
      }

      * {
        font-family: "Nunito", sans-serif;
        box-sizing: border-box;
        margin: 0;
      }

      .main {
        width: min(25rem, 100%);
        background-color: #fff;
        border-radius: 0.3rem;
        padding: 1rem;
        margin: 0 auto;
      }

      h1 {
        text-align: center;
        font-size: 3rem;
        font-weight: 100;
        color: #2980b9;
      }

      h2 {
        text-align: center;
        font-size: 1.5rem;
        font-weight: 100;
        color: #888;
        margin-bottom: 1rem;
      }

      .text {
        font-size: 1rem;
        margin-bottom: 1rem;
        color: #000;
      }

      .text p {
        margin-bottom: 0.5rem;
      }

      .button {
        display: flex;
        justify-content: center;
        margin-bottom: 0.5rem;
      }

      .expires {
        font-size: 0.9rem;
        color: #888;
        text-align: center;
      }

      .btn {
        background-color: #2980b9;
        color: #fff;
        border: none;
        padding: 0.65rem 1rem;
        display: block;
        border-radius: 0.3rem;
        font-size: 1rem;
        cursor: pointer;
        text-decoration: none;
        width: 100%;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <div class="main">
        <h1>SET ONLINE</h1>
        <h2>Reset your password</h2>
        <div class="text">
          <p>
            You are receiving this email because we received a password reset
            request for your account.
          </p>
          <p>
            If you did not request a password reset, no further action is
            required.
          </p>
        </div>
        <p class="button">
          <a href="<%= locals.link %>" class="btn">Reset Password</a>
        </p>
        <div class="expires">Link expires <%= locals.expires %></div>
      </div>
    </div>
  </body>
</html>
